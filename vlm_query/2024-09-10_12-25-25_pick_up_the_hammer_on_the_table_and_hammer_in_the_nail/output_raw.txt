```python
# The task involves two main stages: 
# 1. Grasping the hammer (keypoint 0).
# 2. Hammering the nail (keypoint 1).
# Each stage has specific constraints to ensure the task is completed successfully.

num_stages = 2

### stage 1 sub-goal constraints
def stage1_subgoal_constraint1(end_effector, keypoints):
    """Align the end-effector with the hammer (keypoint 0)."""
    target_keypoint = keypoints[0]
    cost = np.linalg.norm(end_effector - target_keypoint)  # Cost is the distance to the hammer
    return cost

### stage 1 path constraints
# No path constraints needed for the grasping stage.

### stage 2 sub-goal constraints
def stage2_subgoal_constraint1(end_effector, keypoints):
    """Ensure the hammer is positioned correctly to hammer the nail (keypoint 1)."""
    target_keypoint = keypoints[1]
    cost = np.linalg.norm(end_effector - target_keypoint) - 5  # Hammer should be 5cm above the nail
    return cost

### stage 2 path constraints
def stage2_path_constraint1(end_effector, keypoints):
    """Ensure the robot is still grasping the hammer (keypoint 0)."""
    return get_grasping_cost_by_keypoint_idx(0)

# No additional path constraints needed for the hammering stage.

"""
Summarize keypoints to be grasped in all grasping stages.
The length of the list should be equal to the number of stages.
For grasping stage, write the keypoint index. For non-grasping stage, write -1.
"""
grasp_keypoints = [0, -1]

"""
Summarize at **the end of which stage** the robot should release the keypoints.
The keypoint indices must appear in an earlier stage as defined in `grasp_keypoints`.
"""
release_keypoints = [-1, 0]  # Release the hammer after hammering the nail.
```